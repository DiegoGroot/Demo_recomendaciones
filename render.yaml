services:
  - type: web
    name: sira-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    autoDeploy: false
    healthCheckPath: /
    plan: starter
    envVars:
      - key: DB_HOST
        value: mysql-sira-dieguitogroot-mysql.c.aivencloud.com
      - key: DB_USER
        value: avnadmin
      - key: DB_PASSWORD
        sync: false
      - key: DB_NAME
        value: defaultdb
      - key: DB_PORT
        value: "27373"
      - key: DB_SSL
        value: "true"
    ports:
  - port: 8001